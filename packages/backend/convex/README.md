# Convex backend

Add the data model and server functions for Asoebi here when implementing a
feature from [`docs/PRD.md`](../../../docs/PRD.md).

The `_generated` directory is maintained by the Convex CLI and is committed so
the web app can consume the generated API contract.
